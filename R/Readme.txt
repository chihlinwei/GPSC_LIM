Note that running LIM_GC1.R and LIM_GS1.R will generate a different set of solution and thus a different set of network indices. 
To reproduce the figure results, execute the .RMD files in figure_RMD.