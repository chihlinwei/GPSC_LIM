To reproduce the figure results, directly execute the LIM_plot.R and Network_indices_plot.R files.
For the Table results, please refer to .html files. 
Note that if directly execute the LIM.R with .input file, a new set of solution will be produced (and thus different results). 
